services:
  backend:
    image: northsky-migrator-backend:latest
    environment:
      - ENDPOINT=http://host.docker.internal
    ports:
      - 9090:9090
  # db:
  #   image: postgres:latest
  #   environment:
  #     - POSTGRES_PASSWORD=toasty
